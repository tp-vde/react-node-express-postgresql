app:
  title: Student Management System
  version: 1.0.0
  baseUrl: http://localhost:3000
  feature:
    useAuth: false

organization:
  name: My Company

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    allowedHeaders: ['Content-Type', 'Authorization']
    # credentials: true

  database:
    # client: better-sqlite3
    # connection: ':memory:'
    client: pg
    connection:
      host: db
      user: user
      password: password
      database: postgres
      port: 5432
      # domain: string;
      # instanceName: string;
      # debug: boolean;
      # requestTimeout: number;

      
    pool: 
      min: 2
      max: 10
    migrations:
      directory: './migrations'
    seeds:
      directory: './seeds'


  auth:
    keys:
      secret: 3e7b760bf15487a189971d06dbe008dd26ca7cd3c856dd7ead1ae800efda917af2c2194c77fce6f5b2e38dc762ef61a3d578636150b34b937fd7ba8fd72582a3
      expirationTime: '1h'
  # dangerouslyDisableDefaultAuthPolicy: true
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  issuer: https://accounts.google.com
  clientId: your-client-id
  clientSecret: your-client-secret
  callbackUrl: http://localhost:3000/auth/callback
  scopes:
    - openid
    - profile
    - email

  roles:
    - ADMIN
    - USER
    - GUEST
  # jwt:
  #   secret: your-jwt-secret
  #   expiresIn: 1h

  email:
    transporter:
      host: smtp.gmail.com
      port: 587
      secure: false
      auth:
        user: testsvde1@gmail.com
        pass: kdeh fipg rpxe lmxq

    mailOptions:
      from: VILLAGE DE L'EMPLOI
      to: ''
      subject: Initialisation du mot de passe 
      # text: 'Ceci est un test d\'envoi d\'e-mail avec Sendmail.'  ==> Corps du message (texte)
      html: <p>Bonjour, <br/> Ceci est un test d\'envoi d\'e-mail avec <b>Sendmail</b>.</p>,  # Corps du message (HTML)


proxy:
  endpoints:
    '/task/imports':
      target: 'http://localhost:7007'
      changeOrigin: true
